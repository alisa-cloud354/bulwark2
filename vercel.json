{
  "buildCommand": "npm install && npx vite build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://bulwark-eosin.vercel.app/api/:path*"
    }
  ]
}
